import styled from "styled-components";

export const Content = styled.section`
    margin-top: 87px;

    display: grid;
    grid-template-rows: auto;
    row-gap: 46px;
    column-gap: 40px;

    grid-template: "" minmax(auto, auto) / 550px 464px;
`;
