import Header from "../../components/Header";
import * as S from "./styles";

const AccessData = () => {
    const headerText = 10;

    return (
        <S.Wrapper>
            <Header />
        </S.Wrapper>
    );
};

export default AccessData;
